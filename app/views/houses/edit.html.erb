<% content_for :title, "Edit #{@house.name} — GGV Energy" %>

<%= link_to "← #{@house.name}", house_path(@house), class: "text-sm text-slate-500 hover:text-slate-700" %>

<h1 class="mt-2 text-2xl font-semibold text-slate-900">Edit house</h1>

<div class="mt-8 max-w-md">
  <%= render "form", house: @house %>
</div>
